---
name: New cookbook example
about: Propose or claim a runnable example / cookbook
title: "example: <name>"
labels: cookbook
---

**What it shows** (one line):

**Category** (product quickstart / framework cookbook / migration guide / use-case / MCP):

**Template to copy** (`run-code-in-a-sandbox` / `langchain-code-interpreter` / `eve-agent-on-tenki` / new):

**The bar:** ships a `verify.mjs` that runs against a live Tenki sandbox and passes in CI (see [CONTRIBUTING.md](../CONTRIBUTING.md)).

**Companion article:** the example is the canonical code; the walkthrough article goes with it.

**Notes / dependencies** (e.g. needs `allowOutbound`, needs a package on npm, Python harness):
