{
  "name": "openhermit-tenki-cookbook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Runnable OpenHermit-style Tenki exec backend: sticky sessions, pause/resume across gateway restarts, staged skill sync.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "demo": "tsx tenki-agent-sandbox.ts",
    "verify": "tsx verify.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tenkicloud/sandbox": "^0.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
